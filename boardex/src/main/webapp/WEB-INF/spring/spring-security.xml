<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:security="http://www.springframework.org/schema/security"
	xsi:schemaLocation="http://www.springframework.org/schema/security http://www.springframework.org/schema/security/spring-security.xsd
		http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd">

	<bean id="bcryptPasswordEncoder"
		class="org.springframework.security.crypto.bcrypt.BCryptPasswordEncoder" />
	
	
	<security:http auto-config='true' use-expressions="true"> 
		<security:intercept-url pattern="/resources/**" access="permitAll"/>
		<security:intercept-url pattern="/users/**" access="permitAll"/>
		<security:intercept-url pattern="/board/**" access="hasRole('ROLE_USER')" />
		<security:intercept-url pattern="/favicon.ico" access="hasRole('ROLE_ANONYMOUS')" />
		<!-- <security:intercept-url pattern="/**" access="permitAll" /> -->
	
		  <security:form-login login-page="/login/loginpage.do"
        			login-processing-url="/login.do"
        			default-target-url="/"
        			username-parameter="email" 
        			password-parameter="userpw" />
        			
        			 	
        <security:session-management>
        	<security:concurrency-control max-sessions="1" expired-url="/" />
        </security:session-management>
        
	</security:http>
	
	
	<security:authentication-manager>
		<security:authentication-provider>
			 <security:user-service>
				<security:user name="aaa" password="{noop}aaa" authorities="ROLE_USER"></security:user>
				<security:user name="admin" password="{noop}admin" authorities="ROLE_USER, ROLE_ADMIN"></security:user>
			</security:user-service> 
			
		<!-- 	<security:password-encoder hash="bcrypt"/>
			
			<security:jdbc-user-service data-source-ref="dataSource"
				users-by-username-query="SELECT email, user_pw FROM USER WHERE email=?"/>
				
			 -->
		
		</security:authentication-provider>
	</security:authentication-manager>
</beans>